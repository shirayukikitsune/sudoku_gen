load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "sudoku-gen",
    srcs = ["main.cpp"],
    deps = [
        "//src/output",
        "//src/sudoku",
        "@boost//:program_options",
    ],
)
